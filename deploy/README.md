# deploy/

Legacy deployment packs kept for history and comparison.

Default path:

```bash
git clone git@github.com:kpince/openclaw-ops.git
cd openclaw-ops
sudo -E ./install.sh
```

Only use the subdirectories here if you intentionally want an older installation model.
