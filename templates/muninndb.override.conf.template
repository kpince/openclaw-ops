[Service]
Environment="MUNINN_OPENAI_KEY=${INSTALL_MUNINN_OPENAI_KEY}"
Environment="MUNINN_ENRICH_URL=${INSTALL_MUNINN_ENRICH_URL}"
Environment="MUNINN_ENRICH_API_KEY=${INSTALL_MUNINN_ENRICH_API_KEY}"
Environment="MUNINN_MCP_TOKEN=${MUNINN_MCP_TOKEN}"
